- hosts: localhost
  vars:
   host: "192.168.122.40"
   username: "admin"
   password: ""
   vdom: "root"
  tasks:
  - name: Configure virtual pool.
    fortios_switch-controller_virtual-port-pool:
      host:  "{{  host }}"
      username: "{{ username }}"
      password: "{{ password }}"
      vdom:  "{{  vdom }}"
      switch-controller_virtual-port-pool:
        state: "present"
        description: "<your_own_value>"
        name: "default_name_4"
